// função substitui dados para remoção
// function swaping node for removal
/*
void substituiDados(Node **PP, TNoABP **pps) 
{
    TNoABP *esq = (*pp)->esq;
    TNoABP *dir = (*pp)->dir;
    memcpy(*pp, *pps , sizeof(TnoABP));
    (*pp)->esq = esq;
    (*pp)->dir = dir;

    //dessa forma pode-se mexer a vontade na struct e mantem o foco em remover o nó folha
    // fazer remova todos os nó descendentes de menor valor chave de uma ab 
    
}
*/